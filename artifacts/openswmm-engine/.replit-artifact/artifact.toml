kind = "web"
previewPath = "/"
title = "OpenSWMM Engine"
version = "1.0.0"
id = "artifacts/openswmm-engine"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18532

[services.development]
run = "pnpm --filter @workspace/openswmm-engine run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/openswmm-engine", "run", "build" ]
publicDir = "artifacts/openswmm-engine/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18532"
BASE_PATH = "/"
